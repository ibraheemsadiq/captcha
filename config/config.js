
// This is the default configuration
// These values will be used unless they are overwritten by the values in <configFileName>


module.exports = function() {
    var config = {
        "host":"localhost",
        "username":"root",
        "password":	"",
        "database":	"captcha",
        "port": 3000
    };
    return config;
}